apiVersion: v1
kind: Service
metadata:
  name: {{ include "klag-exporter.fullname" . }}
  labels:
    {{- include "klag-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "klag-exporter.selectorLabels" . | nindent 4 }}
